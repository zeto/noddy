extends layout

block head
  script(src='/socket.io/socket.io.js')
    var socket = io.connect('http://localhost');
    socket.on('news', function (data) {
      console.log(data);
      socket.emit('my other event', { my: 'data' });
    });

block content
  h1= title
  p Welcome to #{title}
  p Number of clients: #{clients.size}
